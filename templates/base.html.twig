<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome to My App{% endblock %}</title>
    <link rel="icon" href="/path/to/favicon.ico">
    <style>
        body { font-family: Arial, sans-serif; margin: 0; padding: 0; background: #f4f4f4; color: #333; }
        header { background: #007bff; color: #ffffff; padding: 10px 20px; text-align: center; }
        .container { padding: 20px; }
        footer { background: #333; color: #ffffff; text-align: center; padding: 10px; position: fixed; bottom: 0; width: 100%; }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <header>
        <h1>{% block header %}My App{% endblock %}</h1>
        <div>
        {% if app.user %}
            Logged in as: {{ app.user.username }}
        {% else %}
            Not logged in.
        {% endif %}
    </div>
    </header>

    <div class="container">
        {% block body %}{% endblock %}
    </div>

    <footer>
        {% block footer %}Copyright &copy; {{ "now"|date("Y") }} My App{% endblock %}
    </footer>

    {% block javascripts %}{% endblock %}
</body>
</html>
